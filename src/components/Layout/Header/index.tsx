import React from "react"

import styles from "./index.module.scss"

const HeaderCompany = () => {
    return <div className={styles.headerCompany}>Day la header</div>
}

export default React.memo(HeaderCompany)
